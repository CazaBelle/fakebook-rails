
<%= simple_form_for @post do |f| %>
  <%= f.label :name, 'Your Post', class: "sr-only" %>
  <%= f.input :content, placeholder: "Post something", class: "sr-only" %>
  <%= f.button :submit, class: "btn btn-info" %>
  
<% end %>